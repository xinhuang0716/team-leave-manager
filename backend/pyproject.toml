[project]
name = "backend"
version = "0.3.0"
description = "Backend API for managing team leave records"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.5",
    "fastapi>=0.141.1",
    "uvicorn>=0.52.1",
]
